// Global app configuration
// Toggle DevTools auto-open behavior here for builds.
// Set to true to auto-open DevTools for each BrowserView on startup.
export const DEVTOOLS_AUTO_OPEN: boolean = false;

